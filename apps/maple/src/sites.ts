/**
 * Ссылка на соседний сайт.
 *
 * «Сценика» — отдельное приложение со своей сборкой и своим доменом
 * (apps/scenika). Здесь остаётся только адрес: на проде боевой домен,
 * в разработке — соседний dev-сервер, чтобы переход по ссылке не уводил
 * с локальной копии на живой сайт.
 */

/** Куда ведёт ссылка на «Сценику». */
export const SCENIKA_HOME = import.meta.env.DEV
  ? 'http://localhost:5181/'
  : 'https://scenika.ru/';
